# CW1 local runtime environment template
# Copy to .env when needed and adjust values for your machine.

# PostgreSQL (from repo root docker-compose.yml)
# Host mapping is 5439 -> container 5432.
# POSTGRES_DB is not set in compose, so PostgreSQL default database is "postgres".
POSTGRES_HOST=localhost
POSTGRES_PORT=5439
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# MongoDB (from repo root docker-compose.yml)
# Host mapping is 27019 -> container 27017.
# No Mongo username/password is configured in compose.
MONGO_HOST=localhost
MONGO_PORT=27019
MONGO_DB=ift_cw

# MinIO (from repo root docker-compose.yml)
# Bucket lifecycle is managed by compose service "minio_client_cw":
# it deletes and recreates bucket "csreport", then sets it public.
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=ift_bigdata
MINIO_SECRET_KEY=minio_password
MINIO_BUCKET=csreport

# Optional runtime overrides for environment-specific behavior
# (CLI still has highest priority when provided)
# PIPELINE_FREQUENCY=daily
# PIPELINE_BACKFILL_YEARS=5
# PIPELINE_COMPANY_LIMIT=20
# PIPELINE_ENABLED_EXTRACTORS=source_a,source_b

# Alpha Vantage (paid primary provider)
ALPHA_VANTAGE_API_KEY=YOUR_KEY
