"""
Processing module with VAR, Momentum, and Portfolio selection calculators.

This module handles:
- VAR (Value at Risk) calculation at 95% confidence
- Momentum factors (technical indicators)
- Portfolio selection based on composite scoring
"""

logger_config = """
Configure logging before importing processing modules.
Example:
    import logging
    logging.basicConfig(level=logging.INFO)
"""
