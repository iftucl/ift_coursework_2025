# CW2 environment overrides for config/backtest_config.yaml
# Copy to .env and source before running, or set directly in CI.
# NEVER commit a populated .env file.

# Database — mirrors CW1 postgres_db_cw container defaults
POSTGRES_HOST=localhost
POSTGRES_PORT=5439
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DATABASE=fift
POSTGRES_SCHEMA=systematic_equity
